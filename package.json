{
  "name": "forge",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "happy-dom": "^20.7.0",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
