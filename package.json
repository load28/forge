{
  "name": "forge",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "happy-dom": "^20.7.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
